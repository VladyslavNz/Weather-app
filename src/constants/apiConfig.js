/**
 * API related constants
 */
export const API_ENDPOINTS = {
  OPENWEATHER: "/api/openweather",
};

export const WEATHER_TYPES = {
  CURRENT: "weather",
  FORECAST: "forecast",
};

export const DEFAULT_FORECAST_COUNT = 40;
